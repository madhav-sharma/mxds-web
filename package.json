{
  "name": "mxds-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --debug",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "custom_build": "rm -rf dist && npm run build && npm run preview"
  },
  "devDependencies": {
    "sass": "^1.66.1",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.1",
    "hyperscript.org": "^0.9.11"
  }
}
